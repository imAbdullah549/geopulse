export function DevicesPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Devices</h1>
      <p className="text-muted-foreground mt-2">
        Next: RTK Query + filters + table + pagination (like Alerts).
      </p>
    </div>
  );
}
